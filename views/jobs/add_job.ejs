<%- include ('../partials/header'); %>
<title>Add Job</title>
<%- include ('../partials/navbar'); %>
<link rel="stylesheet" type="text/css" href="/styles/login.css">
	
	<div>
            <div class="container">
                <h2 class="py-2 page_title">Add Job:</h2>
                <form action="/jobs" class="form_content" method="POST">
					<div class="form-group">
                        <input autocomplete="off" autofocus class="form-control" name="job[job_name]" placeholder="Job Name" type="text" required>
                    </div>
					<p>Job Location:</p>
					<div class="form-group">
                        <input autocomplete="off" autofocus class="form-control" name="job[street]" placeholder="Street" type="text">
                    </div>    	
					<div class="row">
						<div class="col-lg-4 form-group">
							<input autocomplete="off" autofocus class="form-control" name="job[country]" placeholder="Country" type="text">
						</div>
						<div class="col-lg-4 form-group">
							<input autocomplete="off" autofocus class="form-control" name="job[city]" placeholder="City" type="text">
						</div>
						<div class="col-lg-4 form-group">
							<input autocomplete="off" autofocus class="form-control" name="job[zip]" placeholder="Zip" type="text">
						</div>
					</div>	
					
					<div class="form-group">
						<p>Associated Client:</p>
						<select class="form-control dropdown_search" name="job[client]">
							<option value="">Select Client</option>
							<% clients.forEach(function(client){ %>							 
								<% if (client.active) { %>	
									<option value="<%= client._id %>">
									<% if (client.organization_name) { %>
									<%= client.organization_name %>
									<% } else { %>
									<%= client.last_name %>, <%= client.first_name %> <%= client.middle_name %>
									<% } %>
								</option>
								<% } %>								   
							<% }); %>
						</select>
                    </div>					
                    <div class="form-group">
						<input type="date" class="form-control" name="job[start_date]" placeholder="Job Start Date" required>
                    </div>
                    <div class="form-group">
						<input type="date" class="form-control" name="job[end_date]" placeholder="Job End Date" required>
                    </div>		
					
					<div class="row">
						<div class="col-lg-4 form-group">
							<span>$</span>
							<div class="billing">
								<input autocomplete="off" autofocus class="form-control" name="job[billing_price]" placeholder="0000.00" type="text">
							</div>
							<label for="job[billing_price]">Format: 0000.00</label>
							
						</div>
					</div>
					
                    <div class="form-group">
						<p>Job Description:</p>
                        <textarea name="job[description]"></textarea>
                    </div> 					

                    <button class="button zoom" type="submit">Submit</button>
                </form>
            </div>
        </div>


<%- include ('../partials/footer'); %>